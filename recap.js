// 1. Variable
var myName = `Mehedi Hasan Himel`;

// math operation   +, -, *, /, %, ++, --
// shortcut for math operation  +=, -=, *=, /=, %=

// 2. Array
var friends = [`Abul`, `Babul`, `Kabul`, `Dabul`];
var thirdFriend = friends[2];
friends[3] = `Dambool`;

// 3. Conditional , conditional symbols: ==, ===, !=, !==, >, <, >=, <=

if (friends.length < 2) {
  console.log(`fokir tor kono friend nai...`);
} else {
  console.log(`tor friend ache...`);
}

// 4. loops

var number = 0; 
while (number <= 6) {
   console.log(number);
   number++
}

for (let i = 0; i < array.length; i++) {
   const element = array[i];
   
}

// 5. function
function isMoonUp(time) {
   if(time>=19 && time<=5){
      return true;
   }
}

// 6. object
var jantus = {
   height: 5.5,
   romanticicm: `Heavy`,
   bapArTaka: `Borolox`,
}